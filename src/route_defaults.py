"""Shared route defaults used by generation and plotting scripts."""

# Keep these aligned with merge #16 defaults to maintain expected routing behavior.
DEFAULT_LOCATION = "London"
DEFAULT_ROUTE_ORIGIN = (51.5565, -0.1646)
DEFAULT_ROUTE_DESTINATION = (51.5943, -0.1302)
